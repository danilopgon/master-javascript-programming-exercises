function getAllLetters(str) {
    // your code here
    let arrayOfLetters = []

    for (let i = 0; i < str.length; i++) {
        arrayOfLetters.push(str[i]);
    }

    return arrayOfLetters;
    
}